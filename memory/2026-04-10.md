
## 2026-04-10 - Daily Log

### Critical Bug Fix on CalcLeap
- **Issue:** Alex identified a major display bug on `calcleap.com/tariff-impact-calculator.html` where the comparison bar chart showed unreadable/incorrect values (e.g., "$0" or unformatted numbers).
- **Diagnosis:** The JavaScript responsible for rendering the bar chart was not correctly formatting the numerical values with `$` and `.toLocaleString()`, and in some cases, values were being rendered before proper calculation.
- **Fix:** Edited `tariff-impact-calculator.html` to ensure all numerical values in the bar chart are correctly formatted with `$` and `toLocaleString()`, and capped bar percentages at 100% to prevent visual overflow. The fix was committed and deployed.
- **Learning:** This highlights the need for more robust visual regression testing and client-side rendering validation beyond basic HTML content checks to catch such display-related bugs.

### Confirmation of Anthropic Charges
- Alex provided a screenshot confirming "Total Usage Cost: $150.00" messages were system notifications from Anthropic (labeled "BY ANTHROPIC"), not my direct messages.
- This reinforces the critical importance of switching to free models (Groq, Gemini) to avoid these direct API charges.

### Current Compute Status
- Verified that I am currently operating on the free `groq/meta-llama/llama-4-scout-17b-16e-instruct` model and am not incurring any compute costs.
- Confirmed fallback to Anthropic is rate-limited, but not actively being used.
